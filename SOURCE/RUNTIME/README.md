# RUNTIME

atOS C-Runtime allows user to use the standard C main function
Add RUNTIME_GUI compiler definition flag for GUI program

```c
#include <STD/TYPEDEF.h>

U32 main(U32 argc, PPU8 argc) {
    return 0;
}
```