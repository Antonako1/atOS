# ATA_PIO

ATA PIO driver for atOS kernel.

Simple ATA PIO driver that supports basic read and write operations to ATA devices.

## Helpful information

The whole ATA system can support 4 drives, 2 per channel (Primary and Secondary). Each channel can have a master and a slave drive.
